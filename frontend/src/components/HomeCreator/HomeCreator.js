import React from "react";
import { useLevel } from "../../hooks/useLevel";
import { LevelCards } from "../LevelCard/LevelCard";
export const HomeCreator = () => {

  return (
    <>
      <h1>Most rated levels</h1>
      <ul>{}</ul>
    </>
  );
};
